#!/usr/bin/env bash
# Live-reloading dev server at http://localhost:8080 — write Markdown, see it instantly.
set -euo pipefail
cd "$(dirname "$0")"
[ -d node_modules ] || npm install
npx @11ty/eleventy --serve "$@"
