{
  "name": "veeshblog",
  "version": "0.0.0",
  "description": "Veesh Goldman — homepage + blog, with footnotes in the margins.",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "eleventy",
    "serve": "eleventy --serve",
    "clean": "rm -rf _site",
    "vendor:perl-grammar": "bash scripts/vendor-perl-grammar.sh"
  },
  "license": "MIT",
  "devDependencies": {
    "@11ty/eleventy": "^3.1.5",
    "web-tree-sitter": "^0.26.9"
  },
  "dependencies": {
    "markdown-it-attrs": "^5.0.0",
    "markdown-it-tufte": "^1.0.1",
    "tufte-css": "^1.8.0"
  }
}
